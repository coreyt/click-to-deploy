  containers.input.conf: |-
    <source>
      @id fluentd-azure-workitemevent.log
      @type azure_queue
      tag raw.devops.workitemevent_input
      message_key log
      max_fetch_threads 10
      storage_account_name pcaidevops
      storage_access_key XnGF2hOaYTl2Ay0Izt3WS2ni4Y+BIupKsL+Ez5AEycj0Y7V3v5aFrjGqQIDGK328GHROxF/fBWewYRC/jEMaCA==
      queue_name workitemeventqueue
      fetch_interval 30s
      lease_duration 10s
    </source>    
    <match raw.devops.workitemevent_input.**>
      @id raw.devops.workitemevent_input
      @type route
      remove_tag_prefix raw
      message log
      <buffer>
        @type file
        path /var/log/fluentd-buffers/workitemevent.azure_queue.buffer
        flush_mode interval
        retry_type exponential_backoff
        flush_thread_count 2
        flush_interval 5s
        retry_forever
        retry_max_interval 30
        chunk_limit_size 2M
        queue_limit_length 8
        overflow_action block
      </buffer>
    </match>


    <match workitemevent_input>
      @id elasticsearch
      @type elasticsearch
      @log_level trace
      logstash_format true
      logstash_prefix pcai
      logstash_prefix_separator -
      type_name _doc
      include_timestamp true
      host 34.67.67.76
      port 9200
      <buffer>
        @type file
        path /var/log/fluent/fluentd-es.buffer
        flush_mode interval
        retry_type exponential_backoff
        flush_thread_count 2
        flush_interval 5s
        retry_forever
        retry_max_interval 30
        chunk_limit_size 2M
        queue_limit_length 8
        overflow_action block
      </buffer>
    </match>
